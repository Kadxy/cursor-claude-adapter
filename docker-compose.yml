services:
  cursor-claude-adapter:
    build: .
    image: cursor-claude-adapter:latest
    container_name: cursor-claude-adapter
    restart: unless-stopped
    # 对外端口由 .env 的 PORT 决定;容器内固定 3000
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    environment:
      - PORT=3000
